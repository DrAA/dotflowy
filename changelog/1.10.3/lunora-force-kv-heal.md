---
"dotflowy": patch
---

Stuck upgraded-sync migrates that reported "complete" while Daily stayed empty can be unstuck from the browser console: force a classic KV re-import (or a full missing-nodes + KV remigrate) instead of no-oping forever.
