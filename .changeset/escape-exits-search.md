---
"dotflowy": minor
---

**Escape toggles search:** from a bullet, Escape opens the filter when idle and clears `?q=` + collapses when open (suggestion popover still closes first). Cmd+F / the magnifier / Cmd+K still open search.
