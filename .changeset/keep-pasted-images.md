---
"dotflowy": patch
---

**Pasted images survive reload.** Clipboard bytes are copied during the paste event and local blob URLs rehydrate after IndexedDB load, so screenshots no longer vanish on refresh.
