---
"dotflowy": minor
---

**Backend on this machine is now the default.** Your outline syncs across browsers via the local Worker Durable Object. Turn on **This browser only** in Settings if you want localStorage/IndexedDB with no backend writes.
